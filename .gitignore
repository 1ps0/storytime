# Local clones for research / cross-port work — do not commit
/opencode/

# OS junk
.DS_Store
**/.DS_Store

# Editor scratch
*.swp
*~

# User-specific storytime state — never commit (BOARD-016);
# extracted templates commit when built
specs/.storytime/cohort/operator-model-*.md
specs/.storytime/cohort/_user.md
specs/.storytime/intents.md

# Derived board state — the fold regenerates it (FIX-004)
board/state.json
